include ../../bundle.pug

+die-if-bad()
- var global = { 'page-name': 'main' };
+get-user-info()

<!DOCTYPE html>
html(lang="en")
   head
      meta(charset="UTF-8")
      meta(name="viewport", content="width=device-width, initial-scale=1.0")
      meta(http-equiv="X-UA-Compatible", content="ie=edge")
      title Main page | Just Field
      +favicon()
   body.row
      +aside()
      main
