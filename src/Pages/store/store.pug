:php
   $PHP = '../'

include ../../bundle.pug

+die-if-bad()
- var global = { 'page-name': 'store' };
+get-user-info()

<!DOCTYPE html>
html(lang="en")
   head
      meta(charset="UTF-8")
      meta(name="viewport", content="width=device-width, initial-scale=1.0")
      meta(http-equiv="X-UA-Compatible", content="ie=edge")
      title Store | Just Field
      +favicon()
   body.row
      +aside()
      main
